A simple web scoreboard for darts cricket game.

It was designed with a minimalist approach, i.e.:
- Only plain HTML/CSS/JS (not even a bundling step)
- No external dependency
- All data is stored client-side